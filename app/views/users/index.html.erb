<% @users.each do |user| %>

    <p><%= user.first_name %></p>
    <p><%= user.last_name %></p>
    <p><%= user.email %></p>

    <a href="<%= users_delete_path(user.id) %>" data-method="DELETE">Supprimer</a>

<% end %>